Git を突っ込んでみよう！

git-osx-installer
http://code.google.com/p/git-osx-installer/

winだと
http://www.koikikukan.com/archives/2010/08/04-235555.php
が判り易いかもしれない。

日本Symfonyユーザー会の解説
http://www.symfony.gr.jp/git/github-howto


からDLが楽らしいので、行ってみたらインテル入ってないとダメみたいじゃん…
URLをいじってみたら、旧verが出て来たので、git-1.5.4.5-osx-leopard-ppc.mpkg.dmg 
ってのがPPC最終型っぽいのでそれを入れてみた。

ついでにiTerm2ってターミナルの拡張appと評判の微妙なgitのGUIであるGitXを入れた。
GitXを起動しGitX-->Enable Terminal Usage…で何かが入ったっぽい。
iTermにgit helpって打ち込むとソレっぽいのが出て来て入ってるのが確認出来る。

https://github.com/から以下の情報で登録してみる


hakuyo
pipism
wMXX71kd
mol@angel-story.net

以下の様なもの(必要な部分のみ抜粋)が出来たけど…

全体設定：
ダウンロード及びインストール　Git
git config --global user.name "Your Name"
git config --global user.email mol@angel-story.net

次の手順：
mkdir pipism
cd pipism
git init
touch README
git add README
git commit -m 'first commit'
git remote add origin git@github.com:hakuyo/pipism.git
git push -u origin master

意味が解らないので見なかった事に！
http://kuroigamen.com/tags/git/
を見ながら「公開鍵」と「秘密鍵」を登録してみた。
うやむやにしていた上記の内容も解説してあったので
ついでに黒い画面でやっつける。

HD/ユーザー/ishii/に
.gitconfig(不可視)
.ssh(不可視)
pipism(フォルダ　中に不可視.gitフォルダあり)
が出来てる！
試しにindex.htmlとREADME.mdを作成、addしてcommitしてpushしたら
(結局それは何したんだよ？？)
https://github.com/hakuyo
に反映されていたのでご満悦！

GitXを起動させてみた所、うまく動いてくれないが、黒い画面でそれっぽくいじれる気に
なっているので、気にしない事にした。
